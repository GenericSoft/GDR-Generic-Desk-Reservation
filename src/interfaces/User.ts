export interface apiUserType {
  email: string;
  password: string;
  firstName?: string;
  lastName?: string;
}

export interface userType {
  userId: string;
  firstName: string;
  lastName: string;
  email: string;
}
